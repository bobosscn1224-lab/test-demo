"""DeckWeaver-enhanced layout reconstruction modules.

Phase 2: shape_detector  — native shape detection + color scheme extraction
Phase 3: quality         — PPTX rendering + SSIM comparison + calibration
Phase 4: text_styler     — font detection + per-character color + table detection
"""
