"""PPT Maker v2 — streamlined PPT creation skill.

Exports the same interface as v1 for drop-in replacement:
    PPTMakerSkill, _sessions, _helper
"""
from .handler import PPTMakerSkill, _sessions, _helper
